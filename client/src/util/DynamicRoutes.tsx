import React from 'react'
// import { useAuthState } from '../context/auth'
import {Routes, Route, Navigate, Outlet} from 'react-router-dom'

// create a protected route so that only logged in users can access the home page




